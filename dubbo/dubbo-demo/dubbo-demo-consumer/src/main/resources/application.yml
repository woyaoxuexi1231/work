spring:
  application:
    name: dubbo-demo-consumer

server:
  port: 8090

dubbo:
  application:
    name: dubbo-demo-consumer
  registry:
    address: "nacos://host.docker.internal:8848?username=nacos&password=nacos"
    check: false
  consumer:
    check: false

logging:
  level:
    com.example.dubbo.demo: DEBUG
