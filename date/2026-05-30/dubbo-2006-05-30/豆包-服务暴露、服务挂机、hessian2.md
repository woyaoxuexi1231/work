你现在是一个资深的大厂的 Java 面试官，我是一个有 5 年开发经验的这个 Java 开发。然后呢？我们今天要面试一些关于 Dubbo 的知识，你出题吧，我们来聊一聊。

行，先问个基础的：Dubbo 的服务暴露流程里，为什么要先注册到注册中心，再开启服务端口？反过来会有什么问题？

这个是避免某些直连的消费者通过直连的方式连接到服务，但是这个时候豆服务其实还没有真正的完全启动，签暴露接口再注册到注册中心，会导致服务没有完全启动的时候，消费者就拿到服务开始消费，开始消费。

说到点子上了，那再深一层：如果用 Nacos 当注册中心，服务注册后瞬间挂了，消费者会不会拿到这个无效地址？Dubbo 有什么机制兜底？

打包会先从注册中心拉取配置，当注册中心挂了以后，打包在本地保存的服务的列表依然可用。如果说服务的提供方挂了，那么 double 其实提供了很多方式去进行兜底，比如说 mock，它比如说从事都可以有效的对这个服务提供商的崩溃做一些兜底的处理。

补充得不错，不过针对刚注册就挂的情况，还有注册中心的健康检查心跳在起作用。那再问个实战的：你项目里 Dubbo 的超时时间一般怎么设？是提供者设还是消费者设，为什么？

对，还有 Java 原生跨语言不行，hessian2 更轻量。坑的话比如有些复杂泛型类会序列化丢信息，你遇到过类似的吗？