# gc-problem-03 配置
server:
  port: 8103

spring:
  application:
    name: gc-problem-03

# 启动命令: mvn spring-boot:run -Dspring-boot.run.jvmArguments="-Xms128m -Xmx128m -XX:+PrintGCDetails -XX:+PrintGCDateStamps -Xloggc:./gc-problem-03.log"
