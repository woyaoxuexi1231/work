（面试官身体微微前倾，放下你的简历，目光锐利）

**“五年了，简历上只写了‘熟悉集合、多线程、泛型、反射、设计模式’。好，既然你敢写，我就敢往深了问。准备好了吗？我们开始。”**

---

**一、集合——连环追问**

- **第一问**：HashMap 在 JDK 8 里用了数组+链表+红黑树，为什么引入红黑树？为什么不直接用二叉搜索树或者 AVL 树？树化的阈值为什么是 8，这个数字是怎么来的？退化为链表的阈值为什么又是 6，中间差值的 2 是干什么用的？
- **第二问（打断，追问）**：别光背结论。扩容时链表是怎么拆分的？高链和低链的 `(e.hash & oldCap) == 0` 这个判断到底利用了什么数学特性？如果我把负载因子从 0.75 调到 2 或 0.5，在时间与空间上到底会发生什么？结合你见过的线上事故，说说什么场景下必须调，为什么默认没人动它。
- **第三问**：你在多线程下用过 HashMap 吗？知不知道在 JDK 7 里 `resize()` 时链表会成环？把成环的过程一步一步在纸上画出来。这种现象在 JDK 8 里还存在吗？为什么数据覆盖的问题依然没有解决？
- **第四问**：那你在并发场景到底用什么？`ConcurrentHashMap` 在 JDK 7 和 JDK 8 的实现分别讲一下。JDK 8 放弃分段锁，改用 CAS + synchronized，为什么？synchronized 在 JDK 8 里已经足够轻量，这个足够轻量到底是谁保证的？`size()` 方法在 JDK 8 是怎么实现的，为什么放弃了 segment 锁模式下的求和方法，改用 baseCount 和 CounterCell？CounterCell 的填充和防止伪共享是怎么做的？
- **第五问（冷笑）**：看起来你好像都知道一点。最后一个集合问题：`ArrayList` 和 `LinkedList` 的 `add()` 方法，分别在头、尾、中间插入，真实的平均复杂度是多少？不要说大 O，说常数级代价、内存分配、CPU 缓存命中率。然后告诉我，`ArrayList` 为什么敢用 `System.arraycopy` 来搬数据，它快在哪儿？

---

**二、多线程——压力测试**

- **第六问**：synchronized 锁升级的过程，从偏向锁到轻量级锁再到重量级锁，每一步触发的条件是什么？偏向锁的撤销是发生在什么时候？当 JVM 进入安全点，看着那个线程的栈帧，它到底做了什么？为什么电商场景里高争抢的锁要**关闭偏向锁**？
- **第七问**：AQS 用过吧？给我在白板上画出 AQS 中 CLH 队列在 `ReentrantLock` 下的入队和唤醒流程。共享模式和独占模式的 `tryAcquireShared`、`doReleaseShared` 是怎么把等待队列里一连串的节点“传播唤醒”的？`CountDownLatch` 和 `Semaphore` 各自的场景有什么陷阱？比如 `CountDownLatch` 能否重用？为什么不要用 `Semaphore` 去做类似 `CyclicBarrier` 的事情？
- **第八问**：你简历里写了线程池，我想问几个生产上会“出血”的细节。线程池提交任务时，核心线程、队列、最大线程、拒绝策略的交互流程，用你实际的经验重新说一遍，别说八股。如果使用**无界队列**，最大线程数这个参数还有意义吗？什么情况下一个线程池**既设置了核心线程数为 10，最大线程数为 20，又用了无界队列**，这种配置可笑在哪里？你如何设计一个线程池监控，在它要崩之前感知并触发告警？
- **第九问**：ThreadLocal 什么情况下会发生内存泄漏？网上说 key 是弱引用，所以会被回收，那 value 为什么泄漏？让你设计一个线程池里的 ThreadLocal 最佳实践，代码怎么写？`InheritableThreadLocal` 能传递上下文，那在线程池复用的场景下，它有什么致命问题？你一般用什么方案代替（比如 TransmittableThreadLocal 的思路）？

---

**三、泛型与反射——基础功有没有锈**

- **第十问**：泛型擦除到底擦了哪里？泛型信息在字节码的 `Signature` 属性、LocalVariableTypeTable 里还有没有？什么时候能通过反射拿到实际的泛型类型？写一段代码，要求获取 `HashMap<String, Integer>` 的 key 的类型。这个过程中 `ParameterizedType` 和 `TypeVariable` 是怎么协作的？
- **第十一问**：PECS 原则，不要只背“Producer extends, Consumer super”。用 `List<? extends Number>` 和 `List<? super Integer>` 给我解释，为什么前者不能 `add`，后者可以 `add` 但取出来只能是 `Object`？这和类型安全的下界、上界约束到底有什么关系？
- **第十二问**：反射创建对象的性能到底差在哪儿？除了 `setAccessible(true)` 之外，你还能说出哪些优化的点？反射的 `inflate` 机制、`MethodAccessor` 的生成、从本地实现到字节码生成的切换，这个过程了解吗？如果让你在一个高并发 RPC 框架里用反射调用方法，你打算怎么缓存？`MethodHandle` 和反射调用相比有什么优势，引入它是不是就一定能代替反射？
- **第十三问（实战）**：用反射实现一个简易的依赖注入容器，能够扫描包下所有带 `@Inject` 的字段并自动装配，还要处理循环依赖。你现在脑子里把类结构和大致的异常栈想清楚，告诉我循环依赖检测怎么设计。给你两分钟，直接说思路，写伪代码。

---

**四、设计模式与模块化——别只会“工厂+策略”**

- **第十四问**：单例模式，DCL 为什么需要 `volatile`？没有 `volatile` 的情况下，分配内存、初始化、引用的重排序是怎么导致另一个线程拿到一个“半初始化”对象的？枚举单例为什么被推荐，它真的绝对防止反射攻击和序列化破坏吗？不用枚举，你还有什么办法挡住反射？
- **第十五问（设计题）**：我现在有一个支付系统，需要对接支付宝、微信、银联，后续还会接更多。不用讲废话，用**设计模式+模块化**的思路，把核心接口和扩展机制描述出来。
    - 追问：如果新增支付通道，是不是只加一个实现类就完事了？你如何保证**新通道的接入不需要改动主业务代码一根毛**？
    - 再追问：如果两个支付通道需要组合使用（比如先试微信，失败降级到银联），你的模块怎么设计？
    - 持续施压：把这个降级逻辑硬编码在 `if-else` 里就是犯罪。告诉我怎么用“责任链”或“策略+路由”做到零代码侵入的编排。
- **第十六问（模块化与规范）**：你说具备模块化设计能力，那你根据什么原则来划分模块？比如一个交易系统，你把它拆成哪些模块？模块间的 API 是怎么暴露的？你怎么强制执行“反向依赖规则”，防止底层模块依赖上层？如果你发现一个新人无视你定下的规范，直接把订单模块的内部 DTO 传给了支付模块做返回值，你在 Code Review 时怎么指出这是灾难，并用什么架构约束（比如 ArchUnit 或模块可见性）让这种破坏无法编译通过？

---

**五、综合实战——性能和可扩展性，现场拆解**

- **第十七问（编码题，甩过来一支马克笔）**：  
  “给你 15 分钟，在白板上用 **纯 Java** 实现一个**固定容量、线程安全的 LRU 缓存**，要求 `get` 和 `put` 的时间复杂度必须是 O(1)。不允许使用 `LinkedHashMap` 直接糊弄。写完之后，跟我解释在超高并发写入时，你的锁粒度会成为瓶颈吗？如果要改成无锁或分段锁，你打算怎么动这代码？”

- **第十八问（场景压力）**：  
  “有一个消息处理模块，上游每秒扔过来 5 万条消息，你需要解析、校验、入库并返回 ACK。单机部署。我说几个硬指标：不能丢消息，延迟必须控制在 50ms 以内，偶尔出现流量尖峰。你打算怎么设计这个模块？从**数据结构和并发模型**选型开始讲，不许提任何中间件名字，就用 JVM 里的东西，把功能、性能、可扩展性的思维完整呈现一遍。我会随时打断你问细节。”

---

**（面试官放下笔，靠在椅背上）**  
**“上面这些问题，如果你能扛到第十八问还不崩，我们再来聊你简历里刻意没写的 JVM、中间件和分布式。——开始吧。”**